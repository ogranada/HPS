# High Performance Status

## Install
Use yarn to install packages:
```bash
yarn install
```

## Run Unit Tests
Use yarn to install packages:
```bash
yarn test
```

## Build Files
Use yarn to install packages:
```bash
yarn gulp
```

## Start Server
Use yarn to install packages:
```bash
yarn serve
```

